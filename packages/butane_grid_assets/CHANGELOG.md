# Changelog

## 0.1.1

- `SessionOrphaned` (grid_runtime 0.2.0-rc.10) is observed, not fatal, in the burn follower composition; the runtime fake implements `exitOutputOf`; floors follow the the_grid wave 2 (butane_flutter-ncxf).

## 0.1.0

First tagged release — the butane burn asset pack (resident burn circuit +
burn station registry) consumed by lunar_station as a git-tag dependency
(`butane_grid_assets-v{{version}}`).
