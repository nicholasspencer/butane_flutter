// Autogenerated from Pigeon (v26.3.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#undef _HAS_EXCEPTIONS

#include "Api.gen.h"

#include <flutter/basic_message_channel.h>
#include <flutter/binary_messenger.h>
#include <flutter/encodable_value.h>
#include <flutter/standard_message_codec.h>

#include <cmath>
#include <limits>
#include <map>
#include <optional>
#include <string>

namespace butane_windows {
using ::flutter::BasicMessageChannel;
using ::flutter::CustomEncodableValue;
using ::flutter::EncodableList;
using ::flutter::EncodableMap;
using ::flutter::EncodableValue;

FlutterError CreateConnectionError(const std::string channel_name) {
  return FlutterError(
      "channel-error",
      "Unable to establish connection on channel: '" + channel_name + "'.",
      EncodableValue(""));
}

namespace {
template<typename T>
bool PigeonInternalDeepEquals(const T& a, const T& b);

bool PigeonInternalDeepEquals(const double& a, const double& b);

template<typename T>
bool PigeonInternalDeepEquals(const std::vector<T>& a, const std::vector<T>& b);

template<typename K, typename V>
bool PigeonInternalDeepEquals(const std::map<K, V>& a, const std::map<K, V>& b);

template<typename T>
bool PigeonInternalDeepEquals(const std::optional<T>& a, const std::optional<T>& b);

template<typename T>
bool PigeonInternalDeepEquals(const std::unique_ptr<T>& a, const std::unique_ptr<T>& b);

bool PigeonInternalDeepEquals(const ::flutter::EncodableValue& a, const ::flutter::EncodableValue& b);

template<typename T>
bool PigeonInternalDeepEquals(const T& a, const T& b) {
  return a == b;
}

template<typename T>
bool PigeonInternalDeepEquals(const std::vector<T>& a, const std::vector<T>& b) {
  if (a.size() != b.size()) {
    return false;
  }
  for (size_t i = 0; i < a.size(); ++i) {
    if (!PigeonInternalDeepEquals(a[i], b[i])) {
      return false;
    }
  }
  return true;
}

template <typename K, typename V>
bool PigeonInternalDeepEquals(const std::map<K, V>& a, const std::map<K, V>& b) {
  if (a.size() != b.size()) {
    return false;
  }
  for (const auto& kv : a) {
    bool found = false;
    for (const auto& b_kv : b) {
      if (PigeonInternalDeepEquals(kv.first, b_kv.first)) {
        if (PigeonInternalDeepEquals(kv.second, b_kv.second)) {
          found = true;
          break;
        } else {
          return false;
        }
      }
    }
    if (!found) {
      return false;
    }
  }
  return true;
}

bool PigeonInternalDeepEquals(const double& a, const double& b) {
  // Normalize -0.0 to 0.0 and handle NaN equality.
  return (a == b) || (std::isnan(a) && std::isnan(b));
}

template<typename T>
bool PigeonInternalDeepEquals(const std::optional<T>& a, const std::optional<T>& b) {
  if (!a && !b) {
    return true;
  }
  if (!a || !b) {
    return false;
  }
  return PigeonInternalDeepEquals(*a, *b);
}

template<typename T>
bool PigeonInternalDeepEquals(const std::unique_ptr<T>& a, const std::unique_ptr<T>& b) {
  if (a.get() == b.get()) {
    return true;
  }
  if (!a || !b) {
    return false;
  }
  return PigeonInternalDeepEquals(*a, *b);
}

bool PigeonInternalDeepEquals(const ::flutter::EncodableValue& a, const ::flutter::EncodableValue& b) {
  if (a.index() != b.index()) {
    return false;
  }
  if (const double* da = std::get_if<double>(&a)) {
    return PigeonInternalDeepEquals(*da, std::get<double>(b));
  } else if (const ::flutter::EncodableList* la = std::get_if<::flutter::EncodableList>(&a)) {
    return PigeonInternalDeepEquals(*la, std::get<::flutter::EncodableList>(b));
  } else if (const ::flutter::EncodableMap* ma = std::get_if<::flutter::EncodableMap>(&a)) {
    return PigeonInternalDeepEquals(*ma, std::get<::flutter::EncodableMap>(b));
  }
  return a == b;
}

template <typename T>
size_t PigeonInternalDeepHash(const T& v);

size_t PigeonInternalDeepHash(const double& v);

template <typename T>
size_t PigeonInternalDeepHash(const std::vector<T>& v);

template <typename K, typename V>
size_t PigeonInternalDeepHash(const std::map<K, V>& v);

template <typename T>
size_t PigeonInternalDeepHash(const std::optional<T>& v);

template <typename T>
size_t PigeonInternalDeepHash(const std::unique_ptr<T>& v);

size_t PigeonInternalDeepHash(const ::flutter::EncodableValue& v);

template <typename T>
size_t PigeonInternalDeepHash(const T& v) {
  return std::hash<T>()(v);
}

template <typename T>
size_t PigeonInternalDeepHash(const std::vector<T>& v) {
  size_t result = 1;
  for (const auto& item : v) {
    result = result * 31 + PigeonInternalDeepHash(item);
  }
  return result;
}

template <typename K, typename V>
size_t PigeonInternalDeepHash(const std::map<K, V>& v) {
  size_t result = 0;
  for (const auto& kv : v) {
    result += ((PigeonInternalDeepHash(kv.first) * 31) ^ PigeonInternalDeepHash(kv.second));
  }
  return result;
}

size_t PigeonInternalDeepHash(const double& v) {
  if (std::isnan(v)) {
    // Normalize NaN to a consistent hash.
    return std::hash<double>()(std::numeric_limits<double>::quiet_NaN());
  }
  if (v == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return std::hash<double>()(0.0);
  }
  return std::hash<double>()(v);
}

template <typename T>
size_t PigeonInternalDeepHash(const std::optional<T>& v) {
  return v ? PigeonInternalDeepHash(*v) : 0;
}

template <typename T>
size_t PigeonInternalDeepHash(const std::unique_ptr<T>& v) {
  return v ? PigeonInternalDeepHash(*v) : 0;
}

size_t PigeonInternalDeepHash(const ::flutter::EncodableValue& v) {
  size_t result = v.index();
  if (const double* dv = std::get_if<double>(&v)) {
    result = result * 31 + PigeonInternalDeepHash(*dv);
  } else if (const ::flutter::EncodableList* lv =
                 std::get_if<::flutter::EncodableList>(&v)) {
    result = result * 31 + PigeonInternalDeepHash(*lv);
  } else if (const ::flutter::EncodableMap* mv =
                 std::get_if<::flutter::EncodableMap>(&v)) {
    result = result * 31 + PigeonInternalDeepHash(*mv);
  } else {
    std::visit(
        [&result](const auto& val) {
          using T = std::decay_t<decltype(val)>;
          if constexpr (!std::is_same_v<T, double> &&
                        !std::is_same_v<T, ::flutter::EncodableList> &&
                        !std::is_same_v<T, ::flutter::EncodableMap> &&
                        !std::is_same_v<T, std::monostate> &&
                        !std::is_same_v<T, ::flutter::CustomEncodableValue>) {
            result = result * 31 + PigeonInternalDeepHash(val);
          }
        },
        v);
  }
  return result;
}

}  // namespace
// ClientSession

ClientSession::ClientSession() {}

ClientSession::ClientSession(
  const std::string* peripheral_identifier,
  const std::string* client_identifier,
  const std::string* adapter_identifier,
  const std::string* restoration_identifier)
 : peripheral_identifier_(peripheral_identifier ? std::optional<std::string>(*peripheral_identifier) : std::nullopt),
    client_identifier_(client_identifier ? std::optional<std::string>(*client_identifier) : std::nullopt),
    adapter_identifier_(adapter_identifier ? std::optional<std::string>(*adapter_identifier) : std::nullopt),
    restoration_identifier_(restoration_identifier ? std::optional<std::string>(*restoration_identifier) : std::nullopt) {}

const std::string* ClientSession::peripheral_identifier() const {
  return peripheral_identifier_ ? &(*peripheral_identifier_) : nullptr;
}

void ClientSession::set_peripheral_identifier(const std::string_view* value_arg) {
  peripheral_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void ClientSession::set_peripheral_identifier(std::string_view value_arg) {
  peripheral_identifier_ = value_arg;
}


const std::string* ClientSession::client_identifier() const {
  return client_identifier_ ? &(*client_identifier_) : nullptr;
}

void ClientSession::set_client_identifier(const std::string_view* value_arg) {
  client_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void ClientSession::set_client_identifier(std::string_view value_arg) {
  client_identifier_ = value_arg;
}


const std::string* ClientSession::adapter_identifier() const {
  return adapter_identifier_ ? &(*adapter_identifier_) : nullptr;
}

void ClientSession::set_adapter_identifier(const std::string_view* value_arg) {
  adapter_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void ClientSession::set_adapter_identifier(std::string_view value_arg) {
  adapter_identifier_ = value_arg;
}


const std::string* ClientSession::restoration_identifier() const {
  return restoration_identifier_ ? &(*restoration_identifier_) : nullptr;
}

void ClientSession::set_restoration_identifier(const std::string_view* value_arg) {
  restoration_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void ClientSession::set_restoration_identifier(std::string_view value_arg) {
  restoration_identifier_ = value_arg;
}


EncodableList ClientSession::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(peripheral_identifier_ ? EncodableValue(*peripheral_identifier_) : EncodableValue());
  list.push_back(client_identifier_ ? EncodableValue(*client_identifier_) : EncodableValue());
  list.push_back(adapter_identifier_ ? EncodableValue(*adapter_identifier_) : EncodableValue());
  list.push_back(restoration_identifier_ ? EncodableValue(*restoration_identifier_) : EncodableValue());
  return list;
}

ClientSession ClientSession::FromEncodableList(const EncodableList& list) {
  ClientSession decoded;
  auto& encodable_peripheral_identifier = list[0];
  if (!encodable_peripheral_identifier.IsNull()) {
    decoded.set_peripheral_identifier(std::get<std::string>(encodable_peripheral_identifier));
  }
  auto& encodable_client_identifier = list[1];
  if (!encodable_client_identifier.IsNull()) {
    decoded.set_client_identifier(std::get<std::string>(encodable_client_identifier));
  }
  auto& encodable_adapter_identifier = list[2];
  if (!encodable_adapter_identifier.IsNull()) {
    decoded.set_adapter_identifier(std::get<std::string>(encodable_adapter_identifier));
  }
  auto& encodable_restoration_identifier = list[3];
  if (!encodable_restoration_identifier.IsNull()) {
    decoded.set_restoration_identifier(std::get<std::string>(encodable_restoration_identifier));
  }
  return decoded;
}

bool ClientSession::operator==(const ClientSession& other) const {
  return PigeonInternalDeepEquals(peripheral_identifier_, other.peripheral_identifier_) && PigeonInternalDeepEquals(client_identifier_, other.client_identifier_) && PigeonInternalDeepEquals(adapter_identifier_, other.adapter_identifier_) && PigeonInternalDeepEquals(restoration_identifier_, other.restoration_identifier_);
}

bool ClientSession::operator!=(const ClientSession& other) const {
  return !(*this == other);
}

size_t ClientSession::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(peripheral_identifier_);
  result = result * 31 + PigeonInternalDeepHash(client_identifier_);
  result = result * 31 + PigeonInternalDeepHash(adapter_identifier_);
  result = result * 31 + PigeonInternalDeepHash(restoration_identifier_);
  return result;
}

size_t PigeonInternalDeepHash(const ClientSession& v) {
  return v.Hash();
}

// PeripheralSession

PeripheralSession::PeripheralSession(const std::string& peripheral_identifier)
 : peripheral_identifier_(peripheral_identifier) {}

PeripheralSession::PeripheralSession(
  const std::string& peripheral_identifier,
  const std::string* client_identifier,
  const std::string* adapter_identifier,
  const std::string* restoration_identifier)
 : peripheral_identifier_(peripheral_identifier),
    client_identifier_(client_identifier ? std::optional<std::string>(*client_identifier) : std::nullopt),
    adapter_identifier_(adapter_identifier ? std::optional<std::string>(*adapter_identifier) : std::nullopt),
    restoration_identifier_(restoration_identifier ? std::optional<std::string>(*restoration_identifier) : std::nullopt) {}

const std::string& PeripheralSession::peripheral_identifier() const {
  return peripheral_identifier_;
}

void PeripheralSession::set_peripheral_identifier(std::string_view value_arg) {
  peripheral_identifier_ = value_arg;
}


const std::string* PeripheralSession::client_identifier() const {
  return client_identifier_ ? &(*client_identifier_) : nullptr;
}

void PeripheralSession::set_client_identifier(const std::string_view* value_arg) {
  client_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralSession::set_client_identifier(std::string_view value_arg) {
  client_identifier_ = value_arg;
}


const std::string* PeripheralSession::adapter_identifier() const {
  return adapter_identifier_ ? &(*adapter_identifier_) : nullptr;
}

void PeripheralSession::set_adapter_identifier(const std::string_view* value_arg) {
  adapter_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralSession::set_adapter_identifier(std::string_view value_arg) {
  adapter_identifier_ = value_arg;
}


const std::string* PeripheralSession::restoration_identifier() const {
  return restoration_identifier_ ? &(*restoration_identifier_) : nullptr;
}

void PeripheralSession::set_restoration_identifier(const std::string_view* value_arg) {
  restoration_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralSession::set_restoration_identifier(std::string_view value_arg) {
  restoration_identifier_ = value_arg;
}


EncodableList PeripheralSession::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(EncodableValue(peripheral_identifier_));
  list.push_back(client_identifier_ ? EncodableValue(*client_identifier_) : EncodableValue());
  list.push_back(adapter_identifier_ ? EncodableValue(*adapter_identifier_) : EncodableValue());
  list.push_back(restoration_identifier_ ? EncodableValue(*restoration_identifier_) : EncodableValue());
  return list;
}

PeripheralSession PeripheralSession::FromEncodableList(const EncodableList& list) {
  PeripheralSession decoded(
    std::get<std::string>(list[0]));
  auto& encodable_client_identifier = list[1];
  if (!encodable_client_identifier.IsNull()) {
    decoded.set_client_identifier(std::get<std::string>(encodable_client_identifier));
  }
  auto& encodable_adapter_identifier = list[2];
  if (!encodable_adapter_identifier.IsNull()) {
    decoded.set_adapter_identifier(std::get<std::string>(encodable_adapter_identifier));
  }
  auto& encodable_restoration_identifier = list[3];
  if (!encodable_restoration_identifier.IsNull()) {
    decoded.set_restoration_identifier(std::get<std::string>(encodable_restoration_identifier));
  }
  return decoded;
}

bool PeripheralSession::operator==(const PeripheralSession& other) const {
  return PigeonInternalDeepEquals(peripheral_identifier_, other.peripheral_identifier_) && PigeonInternalDeepEquals(client_identifier_, other.client_identifier_) && PigeonInternalDeepEquals(adapter_identifier_, other.adapter_identifier_) && PigeonInternalDeepEquals(restoration_identifier_, other.restoration_identifier_);
}

bool PeripheralSession::operator!=(const PeripheralSession& other) const {
  return !(*this == other);
}

size_t PeripheralSession::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(peripheral_identifier_);
  result = result * 31 + PigeonInternalDeepHash(client_identifier_);
  result = result * 31 + PigeonInternalDeepHash(adapter_identifier_);
  result = result * 31 + PigeonInternalDeepHash(restoration_identifier_);
  return result;
}

size_t PigeonInternalDeepHash(const PeripheralSession& v) {
  return v.Hash();
}

// PeripheralManagerSession

PeripheralManagerSession::PeripheralManagerSession() {}

PeripheralManagerSession::PeripheralManagerSession(
  const std::string* client_identifier,
  const std::string* adapter_identifier,
  const std::string* restoration_identifier)
 : client_identifier_(client_identifier ? std::optional<std::string>(*client_identifier) : std::nullopt),
    adapter_identifier_(adapter_identifier ? std::optional<std::string>(*adapter_identifier) : std::nullopt),
    restoration_identifier_(restoration_identifier ? std::optional<std::string>(*restoration_identifier) : std::nullopt) {}

const std::string* PeripheralManagerSession::client_identifier() const {
  return client_identifier_ ? &(*client_identifier_) : nullptr;
}

void PeripheralManagerSession::set_client_identifier(const std::string_view* value_arg) {
  client_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralManagerSession::set_client_identifier(std::string_view value_arg) {
  client_identifier_ = value_arg;
}


const std::string* PeripheralManagerSession::adapter_identifier() const {
  return adapter_identifier_ ? &(*adapter_identifier_) : nullptr;
}

void PeripheralManagerSession::set_adapter_identifier(const std::string_view* value_arg) {
  adapter_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralManagerSession::set_adapter_identifier(std::string_view value_arg) {
  adapter_identifier_ = value_arg;
}


const std::string* PeripheralManagerSession::restoration_identifier() const {
  return restoration_identifier_ ? &(*restoration_identifier_) : nullptr;
}

void PeripheralManagerSession::set_restoration_identifier(const std::string_view* value_arg) {
  restoration_identifier_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void PeripheralManagerSession::set_restoration_identifier(std::string_view value_arg) {
  restoration_identifier_ = value_arg;
}


EncodableList PeripheralManagerSession::ToEncodableList() const {
  EncodableList list;
  list.reserve(3);
  list.push_back(client_identifier_ ? EncodableValue(*client_identifier_) : EncodableValue());
  list.push_back(adapter_identifier_ ? EncodableValue(*adapter_identifier_) : EncodableValue());
  list.push_back(restoration_identifier_ ? EncodableValue(*restoration_identifier_) : EncodableValue());
  return list;
}

PeripheralManagerSession PeripheralManagerSession::FromEncodableList(const EncodableList& list) {
  PeripheralManagerSession decoded;
  auto& encodable_client_identifier = list[0];
  if (!encodable_client_identifier.IsNull()) {
    decoded.set_client_identifier(std::get<std::string>(encodable_client_identifier));
  }
  auto& encodable_adapter_identifier = list[1];
  if (!encodable_adapter_identifier.IsNull()) {
    decoded.set_adapter_identifier(std::get<std::string>(encodable_adapter_identifier));
  }
  auto& encodable_restoration_identifier = list[2];
  if (!encodable_restoration_identifier.IsNull()) {
    decoded.set_restoration_identifier(std::get<std::string>(encodable_restoration_identifier));
  }
  return decoded;
}

bool PeripheralManagerSession::operator==(const PeripheralManagerSession& other) const {
  return PigeonInternalDeepEquals(client_identifier_, other.client_identifier_) && PigeonInternalDeepEquals(adapter_identifier_, other.adapter_identifier_) && PigeonInternalDeepEquals(restoration_identifier_, other.restoration_identifier_);
}

bool PeripheralManagerSession::operator!=(const PeripheralManagerSession& other) const {
  return !(*this == other);
}

size_t PeripheralManagerSession::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(client_identifier_);
  result = result * 31 + PigeonInternalDeepHash(adapter_identifier_);
  result = result * 31 + PigeonInternalDeepHash(restoration_identifier_);
  return result;
}

size_t PigeonInternalDeepHash(const PeripheralManagerSession& v) {
  return v.Hash();
}

// Peripheral

Peripheral::Peripheral(
  const PeripheralSession& session,
  const ConnectionState& state)
 : session_(std::make_unique<PeripheralSession>(session)),
    state_(state) {}

Peripheral::Peripheral(
  const PeripheralSession& session,
  const std::string* name,
  const int64_t* rssi,
  const ConnectionState& state)
 : session_(std::make_unique<PeripheralSession>(session)),
    name_(name ? std::optional<std::string>(*name) : std::nullopt),
    rssi_(rssi ? std::optional<int64_t>(*rssi) : std::nullopt),
    state_(state) {}

Peripheral::Peripheral(const Peripheral& other)
 : session_(std::make_unique<PeripheralSession>(*other.session_)),
    name_(other.name_ ? std::optional<std::string>(*other.name_) : std::nullopt),
    rssi_(other.rssi_ ? std::optional<int64_t>(*other.rssi_) : std::nullopt),
    state_(other.state_) {}

Peripheral& Peripheral::operator=(const Peripheral& other) {
  session_ = std::make_unique<PeripheralSession>(*other.session_);
  name_ = other.name_;
  rssi_ = other.rssi_;
  state_ = other.state_;
  return *this;
}

const PeripheralSession& Peripheral::session() const {
  return *session_;
}

void Peripheral::set_session(const PeripheralSession& value_arg) {
  session_ = std::make_unique<PeripheralSession>(value_arg);
}


const std::string* Peripheral::name() const {
  return name_ ? &(*name_) : nullptr;
}

void Peripheral::set_name(const std::string_view* value_arg) {
  name_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void Peripheral::set_name(std::string_view value_arg) {
  name_ = value_arg;
}


const int64_t* Peripheral::rssi() const {
  return rssi_ ? &(*rssi_) : nullptr;
}

void Peripheral::set_rssi(const int64_t* value_arg) {
  rssi_ = value_arg ? std::optional<int64_t>(*value_arg) : std::nullopt;
}

void Peripheral::set_rssi(int64_t value_arg) {
  rssi_ = value_arg;
}


const ConnectionState& Peripheral::state() const {
  return state_;
}

void Peripheral::set_state(const ConnectionState& value_arg) {
  state_ = value_arg;
}


EncodableList Peripheral::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(CustomEncodableValue(*session_));
  list.push_back(name_ ? EncodableValue(*name_) : EncodableValue());
  list.push_back(rssi_ ? EncodableValue(*rssi_) : EncodableValue());
  list.push_back(CustomEncodableValue(state_));
  return list;
}

Peripheral Peripheral::FromEncodableList(const EncodableList& list) {
  Peripheral decoded(
    std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(list[0])),
    std::any_cast<const ConnectionState&>(std::get<CustomEncodableValue>(list[3])));
  auto& encodable_name = list[1];
  if (!encodable_name.IsNull()) {
    decoded.set_name(std::get<std::string>(encodable_name));
  }
  auto& encodable_rssi = list[2];
  if (!encodable_rssi.IsNull()) {
    decoded.set_rssi(std::get<int64_t>(encodable_rssi));
  }
  return decoded;
}

bool Peripheral::operator==(const Peripheral& other) const {
  return PigeonInternalDeepEquals(session_, other.session_) && PigeonInternalDeepEquals(name_, other.name_) && PigeonInternalDeepEquals(rssi_, other.rssi_) && PigeonInternalDeepEquals(state_, other.state_);
}

bool Peripheral::operator!=(const Peripheral& other) const {
  return !(*this == other);
}

size_t Peripheral::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(session_);
  result = result * 31 + PigeonInternalDeepHash(name_);
  result = result * 31 + PigeonInternalDeepHash(rssi_);
  result = result * 31 + PigeonInternalDeepHash(state_);
  return result;
}

size_t PigeonInternalDeepHash(const Peripheral& v) {
  return v.Hash();
}

// AdvertisementData

AdvertisementData::AdvertisementData() {}

AdvertisementData::AdvertisementData(
  const std::string* local_name,
  const std::vector<uint8_t>* manufacturer_data,
  const EncodableList* service_uuids,
  const EncodableMap* service_data,
  const int64_t* tx_power_level,
  const bool* is_connectable)
 : local_name_(local_name ? std::optional<std::string>(*local_name) : std::nullopt),
    manufacturer_data_(manufacturer_data ? std::optional<std::vector<uint8_t>>(*manufacturer_data) : std::nullopt),
    service_uuids_(service_uuids ? std::optional<EncodableList>(*service_uuids) : std::nullopt),
    service_data_(service_data ? std::optional<EncodableMap>(*service_data) : std::nullopt),
    tx_power_level_(tx_power_level ? std::optional<int64_t>(*tx_power_level) : std::nullopt),
    is_connectable_(is_connectable ? std::optional<bool>(*is_connectable) : std::nullopt) {}

const std::string* AdvertisementData::local_name() const {
  return local_name_ ? &(*local_name_) : nullptr;
}

void AdvertisementData::set_local_name(const std::string_view* value_arg) {
  local_name_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_local_name(std::string_view value_arg) {
  local_name_ = value_arg;
}


const std::vector<uint8_t>* AdvertisementData::manufacturer_data() const {
  return manufacturer_data_ ? &(*manufacturer_data_) : nullptr;
}

void AdvertisementData::set_manufacturer_data(const std::vector<uint8_t>* value_arg) {
  manufacturer_data_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_manufacturer_data(const std::vector<uint8_t>& value_arg) {
  manufacturer_data_ = value_arg;
}


const EncodableList* AdvertisementData::service_uuids() const {
  return service_uuids_ ? &(*service_uuids_) : nullptr;
}

void AdvertisementData::set_service_uuids(const EncodableList* value_arg) {
  service_uuids_ = value_arg ? std::optional<EncodableList>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_service_uuids(const EncodableList& value_arg) {
  service_uuids_ = value_arg;
}


const EncodableMap* AdvertisementData::service_data() const {
  return service_data_ ? &(*service_data_) : nullptr;
}

void AdvertisementData::set_service_data(const EncodableMap* value_arg) {
  service_data_ = value_arg ? std::optional<EncodableMap>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_service_data(const EncodableMap& value_arg) {
  service_data_ = value_arg;
}


const int64_t* AdvertisementData::tx_power_level() const {
  return tx_power_level_ ? &(*tx_power_level_) : nullptr;
}

void AdvertisementData::set_tx_power_level(const int64_t* value_arg) {
  tx_power_level_ = value_arg ? std::optional<int64_t>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_tx_power_level(int64_t value_arg) {
  tx_power_level_ = value_arg;
}


const bool* AdvertisementData::is_connectable() const {
  return is_connectable_ ? &(*is_connectable_) : nullptr;
}

void AdvertisementData::set_is_connectable(const bool* value_arg) {
  is_connectable_ = value_arg ? std::optional<bool>(*value_arg) : std::nullopt;
}

void AdvertisementData::set_is_connectable(bool value_arg) {
  is_connectable_ = value_arg;
}


EncodableList AdvertisementData::ToEncodableList() const {
  EncodableList list;
  list.reserve(6);
  list.push_back(local_name_ ? EncodableValue(*local_name_) : EncodableValue());
  list.push_back(manufacturer_data_ ? EncodableValue(*manufacturer_data_) : EncodableValue());
  list.push_back(service_uuids_ ? EncodableValue(*service_uuids_) : EncodableValue());
  list.push_back(service_data_ ? EncodableValue(*service_data_) : EncodableValue());
  list.push_back(tx_power_level_ ? EncodableValue(*tx_power_level_) : EncodableValue());
  list.push_back(is_connectable_ ? EncodableValue(*is_connectable_) : EncodableValue());
  return list;
}

AdvertisementData AdvertisementData::FromEncodableList(const EncodableList& list) {
  AdvertisementData decoded;
  auto& encodable_local_name = list[0];
  if (!encodable_local_name.IsNull()) {
    decoded.set_local_name(std::get<std::string>(encodable_local_name));
  }
  auto& encodable_manufacturer_data = list[1];
  if (!encodable_manufacturer_data.IsNull()) {
    decoded.set_manufacturer_data(std::get<std::vector<uint8_t>>(encodable_manufacturer_data));
  }
  auto& encodable_service_uuids = list[2];
  if (!encodable_service_uuids.IsNull()) {
    decoded.set_service_uuids(std::get<EncodableList>(encodable_service_uuids));
  }
  auto& encodable_service_data = list[3];
  if (!encodable_service_data.IsNull()) {
    decoded.set_service_data(std::get<EncodableMap>(encodable_service_data));
  }
  auto& encodable_tx_power_level = list[4];
  if (!encodable_tx_power_level.IsNull()) {
    decoded.set_tx_power_level(std::get<int64_t>(encodable_tx_power_level));
  }
  auto& encodable_is_connectable = list[5];
  if (!encodable_is_connectable.IsNull()) {
    decoded.set_is_connectable(std::get<bool>(encodable_is_connectable));
  }
  return decoded;
}

bool AdvertisementData::operator==(const AdvertisementData& other) const {
  return PigeonInternalDeepEquals(local_name_, other.local_name_) && PigeonInternalDeepEquals(manufacturer_data_, other.manufacturer_data_) && PigeonInternalDeepEquals(service_uuids_, other.service_uuids_) && PigeonInternalDeepEquals(service_data_, other.service_data_) && PigeonInternalDeepEquals(tx_power_level_, other.tx_power_level_) && PigeonInternalDeepEquals(is_connectable_, other.is_connectable_);
}

bool AdvertisementData::operator!=(const AdvertisementData& other) const {
  return !(*this == other);
}

size_t AdvertisementData::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(local_name_);
  result = result * 31 + PigeonInternalDeepHash(manufacturer_data_);
  result = result * 31 + PigeonInternalDeepHash(service_uuids_);
  result = result * 31 + PigeonInternalDeepHash(service_data_);
  result = result * 31 + PigeonInternalDeepHash(tx_power_level_);
  result = result * 31 + PigeonInternalDeepHash(is_connectable_);
  return result;
}

size_t PigeonInternalDeepHash(const AdvertisementData& v) {
  return v.Hash();
}

// ScanResult

ScanResult::ScanResult(
  const Peripheral& peripheral,
  const AdvertisementData& advertisement_data)
 : peripheral_(std::make_unique<Peripheral>(peripheral)),
    advertisement_data_(std::make_unique<AdvertisementData>(advertisement_data)) {}

ScanResult::ScanResult(const ScanResult& other)
 : peripheral_(std::make_unique<Peripheral>(*other.peripheral_)),
    advertisement_data_(std::make_unique<AdvertisementData>(*other.advertisement_data_)) {}

ScanResult& ScanResult::operator=(const ScanResult& other) {
  peripheral_ = std::make_unique<Peripheral>(*other.peripheral_);
  advertisement_data_ = std::make_unique<AdvertisementData>(*other.advertisement_data_);
  return *this;
}

const Peripheral& ScanResult::peripheral() const {
  return *peripheral_;
}

void ScanResult::set_peripheral(const Peripheral& value_arg) {
  peripheral_ = std::make_unique<Peripheral>(value_arg);
}


const AdvertisementData& ScanResult::advertisement_data() const {
  return *advertisement_data_;
}

void ScanResult::set_advertisement_data(const AdvertisementData& value_arg) {
  advertisement_data_ = std::make_unique<AdvertisementData>(value_arg);
}


EncodableList ScanResult::ToEncodableList() const {
  EncodableList list;
  list.reserve(2);
  list.push_back(CustomEncodableValue(*peripheral_));
  list.push_back(CustomEncodableValue(*advertisement_data_));
  return list;
}

ScanResult ScanResult::FromEncodableList(const EncodableList& list) {
  ScanResult decoded(
    std::any_cast<const Peripheral&>(std::get<CustomEncodableValue>(list[0])),
    std::any_cast<const AdvertisementData&>(std::get<CustomEncodableValue>(list[1])));
  return decoded;
}

bool ScanResult::operator==(const ScanResult& other) const {
  return PigeonInternalDeepEquals(peripheral_, other.peripheral_) && PigeonInternalDeepEquals(advertisement_data_, other.advertisement_data_);
}

bool ScanResult::operator!=(const ScanResult& other) const {
  return !(*this == other);
}

size_t ScanResult::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(peripheral_);
  result = result * 31 + PigeonInternalDeepHash(advertisement_data_);
  return result;
}

size_t PigeonInternalDeepHash(const ScanResult& v) {
  return v.Hash();
}

// Service

Service::Service(
  const std::string& uuid,
  bool is_primary)
 : uuid_(uuid),
    is_primary_(is_primary) {}

const std::string& Service::uuid() const {
  return uuid_;
}

void Service::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


bool Service::is_primary() const {
  return is_primary_;
}

void Service::set_is_primary(bool value_arg) {
  is_primary_ = value_arg;
}


EncodableList Service::ToEncodableList() const {
  EncodableList list;
  list.reserve(2);
  list.push_back(EncodableValue(uuid_));
  list.push_back(EncodableValue(is_primary_));
  return list;
}

Service Service::FromEncodableList(const EncodableList& list) {
  Service decoded(
    std::get<std::string>(list[0]),
    std::get<bool>(list[1]));
  return decoded;
}

bool Service::operator==(const Service& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(is_primary_, other.is_primary_);
}

bool Service::operator!=(const Service& other) const {
  return !(*this == other);
}

size_t Service::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(is_primary_);
  return result;
}

size_t PigeonInternalDeepHash(const Service& v) {
  return v.Hash();
}

// Characteristic

Characteristic::Characteristic(const std::string& uuid)
 : uuid_(uuid) {}

Characteristic::Characteristic(
  const std::string& uuid,
  const std::vector<uint8_t>* value,
  const EncodableList* descriptors,
  const CharacteristicProperty* properties)
 : uuid_(uuid),
    value_(value ? std::optional<std::vector<uint8_t>>(*value) : std::nullopt),
    descriptors_(descriptors ? std::optional<EncodableList>(*descriptors) : std::nullopt),
    properties_(properties ? std::make_unique<CharacteristicProperty>(*properties) : nullptr) {}

Characteristic::Characteristic(const Characteristic& other)
 : uuid_(other.uuid_),
    value_(other.value_ ? std::optional<std::vector<uint8_t>>(*other.value_) : std::nullopt),
    descriptors_(other.descriptors_ ? std::optional<EncodableList>(*other.descriptors_) : std::nullopt),
    properties_(other.properties_ ? std::make_unique<CharacteristicProperty>(*other.properties_) : nullptr) {}

Characteristic& Characteristic::operator=(const Characteristic& other) {
  uuid_ = other.uuid_;
  value_ = other.value_;
  descriptors_ = other.descriptors_;
  properties_ = other.properties_ ? std::make_unique<CharacteristicProperty>(*other.properties_) : nullptr;
  return *this;
}

const std::string& Characteristic::uuid() const {
  return uuid_;
}

void Characteristic::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


const std::vector<uint8_t>* Characteristic::value() const {
  return value_ ? &(*value_) : nullptr;
}

void Characteristic::set_value(const std::vector<uint8_t>* value_arg) {
  value_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void Characteristic::set_value(const std::vector<uint8_t>& value_arg) {
  value_ = value_arg;
}


const EncodableList* Characteristic::descriptors() const {
  return descriptors_ ? &(*descriptors_) : nullptr;
}

void Characteristic::set_descriptors(const EncodableList* value_arg) {
  descriptors_ = value_arg ? std::optional<EncodableList>(*value_arg) : std::nullopt;
}

void Characteristic::set_descriptors(const EncodableList& value_arg) {
  descriptors_ = value_arg;
}


const CharacteristicProperty* Characteristic::properties() const {
  return properties_.get();
}

void Characteristic::set_properties(const CharacteristicProperty* value_arg) {
  properties_ = value_arg ? std::make_unique<CharacteristicProperty>(*value_arg) : nullptr;
}

void Characteristic::set_properties(const CharacteristicProperty& value_arg) {
  properties_ = std::make_unique<CharacteristicProperty>(value_arg);
}


EncodableList Characteristic::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(EncodableValue(uuid_));
  list.push_back(value_ ? EncodableValue(*value_) : EncodableValue());
  list.push_back(descriptors_ ? EncodableValue(*descriptors_) : EncodableValue());
  list.push_back(properties_ ? CustomEncodableValue(*properties_) : EncodableValue());
  return list;
}

Characteristic Characteristic::FromEncodableList(const EncodableList& list) {
  Characteristic decoded(
    std::get<std::string>(list[0]));
  auto& encodable_value = list[1];
  if (!encodable_value.IsNull()) {
    decoded.set_value(std::get<std::vector<uint8_t>>(encodable_value));
  }
  auto& encodable_descriptors = list[2];
  if (!encodable_descriptors.IsNull()) {
    decoded.set_descriptors(std::get<EncodableList>(encodable_descriptors));
  }
  auto& encodable_properties = list[3];
  if (!encodable_properties.IsNull()) {
    decoded.set_properties(std::any_cast<const CharacteristicProperty&>(std::get<CustomEncodableValue>(encodable_properties)));
  }
  return decoded;
}

bool Characteristic::operator==(const Characteristic& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(value_, other.value_) && PigeonInternalDeepEquals(descriptors_, other.descriptors_) && PigeonInternalDeepEquals(properties_, other.properties_);
}

bool Characteristic::operator!=(const Characteristic& other) const {
  return !(*this == other);
}

size_t Characteristic::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(value_);
  result = result * 31 + PigeonInternalDeepHash(descriptors_);
  result = result * 31 + PigeonInternalDeepHash(properties_);
  return result;
}

size_t PigeonInternalDeepHash(const Characteristic& v) {
  return v.Hash();
}

// Descriptor

Descriptor::Descriptor(const std::string& uuid)
 : uuid_(uuid) {}

Descriptor::Descriptor(
  const std::string& uuid,
  const std::vector<uint8_t>* value)
 : uuid_(uuid),
    value_(value ? std::optional<std::vector<uint8_t>>(*value) : std::nullopt) {}

const std::string& Descriptor::uuid() const {
  return uuid_;
}

void Descriptor::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


const std::vector<uint8_t>* Descriptor::value() const {
  return value_ ? &(*value_) : nullptr;
}

void Descriptor::set_value(const std::vector<uint8_t>* value_arg) {
  value_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void Descriptor::set_value(const std::vector<uint8_t>& value_arg) {
  value_ = value_arg;
}


EncodableList Descriptor::ToEncodableList() const {
  EncodableList list;
  list.reserve(2);
  list.push_back(EncodableValue(uuid_));
  list.push_back(value_ ? EncodableValue(*value_) : EncodableValue());
  return list;
}

Descriptor Descriptor::FromEncodableList(const EncodableList& list) {
  Descriptor decoded(
    std::get<std::string>(list[0]));
  auto& encodable_value = list[1];
  if (!encodable_value.IsNull()) {
    decoded.set_value(std::get<std::vector<uint8_t>>(encodable_value));
  }
  return decoded;
}

bool Descriptor::operator==(const Descriptor& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(value_, other.value_);
}

bool Descriptor::operator!=(const Descriptor& other) const {
  return !(*this == other);
}

size_t Descriptor::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(value_);
  return result;
}

size_t PigeonInternalDeepHash(const Descriptor& v) {
  return v.Hash();
}

// CharacteristicProperty

CharacteristicProperty::CharacteristicProperty(
  bool broadcast,
  bool read,
  bool write_without_response,
  bool write,
  bool notify,
  bool indicate,
  bool authenticated_signed_writes,
  bool extended_properties,
  bool notify_encryption_required,
  bool indicate_encryption_required)
 : broadcast_(broadcast),
    read_(read),
    write_without_response_(write_without_response),
    write_(write),
    notify_(notify),
    indicate_(indicate),
    authenticated_signed_writes_(authenticated_signed_writes),
    extended_properties_(extended_properties),
    notify_encryption_required_(notify_encryption_required),
    indicate_encryption_required_(indicate_encryption_required) {}

bool CharacteristicProperty::broadcast() const {
  return broadcast_;
}

void CharacteristicProperty::set_broadcast(bool value_arg) {
  broadcast_ = value_arg;
}


bool CharacteristicProperty::read() const {
  return read_;
}

void CharacteristicProperty::set_read(bool value_arg) {
  read_ = value_arg;
}


bool CharacteristicProperty::write_without_response() const {
  return write_without_response_;
}

void CharacteristicProperty::set_write_without_response(bool value_arg) {
  write_without_response_ = value_arg;
}


bool CharacteristicProperty::write() const {
  return write_;
}

void CharacteristicProperty::set_write(bool value_arg) {
  write_ = value_arg;
}


bool CharacteristicProperty::notify() const {
  return notify_;
}

void CharacteristicProperty::set_notify(bool value_arg) {
  notify_ = value_arg;
}


bool CharacteristicProperty::indicate() const {
  return indicate_;
}

void CharacteristicProperty::set_indicate(bool value_arg) {
  indicate_ = value_arg;
}


bool CharacteristicProperty::authenticated_signed_writes() const {
  return authenticated_signed_writes_;
}

void CharacteristicProperty::set_authenticated_signed_writes(bool value_arg) {
  authenticated_signed_writes_ = value_arg;
}


bool CharacteristicProperty::extended_properties() const {
  return extended_properties_;
}

void CharacteristicProperty::set_extended_properties(bool value_arg) {
  extended_properties_ = value_arg;
}


bool CharacteristicProperty::notify_encryption_required() const {
  return notify_encryption_required_;
}

void CharacteristicProperty::set_notify_encryption_required(bool value_arg) {
  notify_encryption_required_ = value_arg;
}


bool CharacteristicProperty::indicate_encryption_required() const {
  return indicate_encryption_required_;
}

void CharacteristicProperty::set_indicate_encryption_required(bool value_arg) {
  indicate_encryption_required_ = value_arg;
}


EncodableList CharacteristicProperty::ToEncodableList() const {
  EncodableList list;
  list.reserve(10);
  list.push_back(EncodableValue(broadcast_));
  list.push_back(EncodableValue(read_));
  list.push_back(EncodableValue(write_without_response_));
  list.push_back(EncodableValue(write_));
  list.push_back(EncodableValue(notify_));
  list.push_back(EncodableValue(indicate_));
  list.push_back(EncodableValue(authenticated_signed_writes_));
  list.push_back(EncodableValue(extended_properties_));
  list.push_back(EncodableValue(notify_encryption_required_));
  list.push_back(EncodableValue(indicate_encryption_required_));
  return list;
}

CharacteristicProperty CharacteristicProperty::FromEncodableList(const EncodableList& list) {
  CharacteristicProperty decoded(
    std::get<bool>(list[0]),
    std::get<bool>(list[1]),
    std::get<bool>(list[2]),
    std::get<bool>(list[3]),
    std::get<bool>(list[4]),
    std::get<bool>(list[5]),
    std::get<bool>(list[6]),
    std::get<bool>(list[7]),
    std::get<bool>(list[8]),
    std::get<bool>(list[9]));
  return decoded;
}

bool CharacteristicProperty::operator==(const CharacteristicProperty& other) const {
  return PigeonInternalDeepEquals(broadcast_, other.broadcast_) && PigeonInternalDeepEquals(read_, other.read_) && PigeonInternalDeepEquals(write_without_response_, other.write_without_response_) && PigeonInternalDeepEquals(write_, other.write_) && PigeonInternalDeepEquals(notify_, other.notify_) && PigeonInternalDeepEquals(indicate_, other.indicate_) && PigeonInternalDeepEquals(authenticated_signed_writes_, other.authenticated_signed_writes_) && PigeonInternalDeepEquals(extended_properties_, other.extended_properties_) && PigeonInternalDeepEquals(notify_encryption_required_, other.notify_encryption_required_) && PigeonInternalDeepEquals(indicate_encryption_required_, other.indicate_encryption_required_);
}

bool CharacteristicProperty::operator!=(const CharacteristicProperty& other) const {
  return !(*this == other);
}

size_t CharacteristicProperty::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(broadcast_);
  result = result * 31 + PigeonInternalDeepHash(read_);
  result = result * 31 + PigeonInternalDeepHash(write_without_response_);
  result = result * 31 + PigeonInternalDeepHash(write_);
  result = result * 31 + PigeonInternalDeepHash(notify_);
  result = result * 31 + PigeonInternalDeepHash(indicate_);
  result = result * 31 + PigeonInternalDeepHash(authenticated_signed_writes_);
  result = result * 31 + PigeonInternalDeepHash(extended_properties_);
  result = result * 31 + PigeonInternalDeepHash(notify_encryption_required_);
  result = result * 31 + PigeonInternalDeepHash(indicate_encryption_required_);
  return result;
}

size_t PigeonInternalDeepHash(const CharacteristicProperty& v) {
  return v.Hash();
}

// CharacteristicPermission

CharacteristicPermission::CharacteristicPermission(
  bool readable,
  bool writeable,
  bool read_encryption_required,
  bool write_encryption_required)
 : readable_(readable),
    writeable_(writeable),
    read_encryption_required_(read_encryption_required),
    write_encryption_required_(write_encryption_required) {}

bool CharacteristicPermission::readable() const {
  return readable_;
}

void CharacteristicPermission::set_readable(bool value_arg) {
  readable_ = value_arg;
}


bool CharacteristicPermission::writeable() const {
  return writeable_;
}

void CharacteristicPermission::set_writeable(bool value_arg) {
  writeable_ = value_arg;
}


bool CharacteristicPermission::read_encryption_required() const {
  return read_encryption_required_;
}

void CharacteristicPermission::set_read_encryption_required(bool value_arg) {
  read_encryption_required_ = value_arg;
}


bool CharacteristicPermission::write_encryption_required() const {
  return write_encryption_required_;
}

void CharacteristicPermission::set_write_encryption_required(bool value_arg) {
  write_encryption_required_ = value_arg;
}


EncodableList CharacteristicPermission::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(EncodableValue(readable_));
  list.push_back(EncodableValue(writeable_));
  list.push_back(EncodableValue(read_encryption_required_));
  list.push_back(EncodableValue(write_encryption_required_));
  return list;
}

CharacteristicPermission CharacteristicPermission::FromEncodableList(const EncodableList& list) {
  CharacteristicPermission decoded(
    std::get<bool>(list[0]),
    std::get<bool>(list[1]),
    std::get<bool>(list[2]),
    std::get<bool>(list[3]));
  return decoded;
}

bool CharacteristicPermission::operator==(const CharacteristicPermission& other) const {
  return PigeonInternalDeepEquals(readable_, other.readable_) && PigeonInternalDeepEquals(writeable_, other.writeable_) && PigeonInternalDeepEquals(read_encryption_required_, other.read_encryption_required_) && PigeonInternalDeepEquals(write_encryption_required_, other.write_encryption_required_);
}

bool CharacteristicPermission::operator!=(const CharacteristicPermission& other) const {
  return !(*this == other);
}

size_t CharacteristicPermission::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(readable_);
  result = result * 31 + PigeonInternalDeepHash(writeable_);
  result = result * 31 + PigeonInternalDeepHash(read_encryption_required_);
  result = result * 31 + PigeonInternalDeepHash(write_encryption_required_);
  return result;
}

size_t PigeonInternalDeepHash(const CharacteristicPermission& v) {
  return v.Hash();
}

// AttRequest

AttRequest::AttRequest(
  int64_t request_id,
  const std::string& central_identifier,
  const std::string& characteristic_uuid,
  const std::string& service_uuid,
  int64_t offset)
 : request_id_(request_id),
    central_identifier_(central_identifier),
    characteristic_uuid_(characteristic_uuid),
    service_uuid_(service_uuid),
    offset_(offset) {}

AttRequest::AttRequest(
  int64_t request_id,
  const std::string& central_identifier,
  const std::string& characteristic_uuid,
  const std::string& service_uuid,
  int64_t offset,
  const std::vector<uint8_t>* value)
 : request_id_(request_id),
    central_identifier_(central_identifier),
    characteristic_uuid_(characteristic_uuid),
    service_uuid_(service_uuid),
    offset_(offset),
    value_(value ? std::optional<std::vector<uint8_t>>(*value) : std::nullopt) {}

int64_t AttRequest::request_id() const {
  return request_id_;
}

void AttRequest::set_request_id(int64_t value_arg) {
  request_id_ = value_arg;
}


const std::string& AttRequest::central_identifier() const {
  return central_identifier_;
}

void AttRequest::set_central_identifier(std::string_view value_arg) {
  central_identifier_ = value_arg;
}


const std::string& AttRequest::characteristic_uuid() const {
  return characteristic_uuid_;
}

void AttRequest::set_characteristic_uuid(std::string_view value_arg) {
  characteristic_uuid_ = value_arg;
}


const std::string& AttRequest::service_uuid() const {
  return service_uuid_;
}

void AttRequest::set_service_uuid(std::string_view value_arg) {
  service_uuid_ = value_arg;
}


int64_t AttRequest::offset() const {
  return offset_;
}

void AttRequest::set_offset(int64_t value_arg) {
  offset_ = value_arg;
}


const std::vector<uint8_t>* AttRequest::value() const {
  return value_ ? &(*value_) : nullptr;
}

void AttRequest::set_value(const std::vector<uint8_t>* value_arg) {
  value_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void AttRequest::set_value(const std::vector<uint8_t>& value_arg) {
  value_ = value_arg;
}


EncodableList AttRequest::ToEncodableList() const {
  EncodableList list;
  list.reserve(6);
  list.push_back(EncodableValue(request_id_));
  list.push_back(EncodableValue(central_identifier_));
  list.push_back(EncodableValue(characteristic_uuid_));
  list.push_back(EncodableValue(service_uuid_));
  list.push_back(EncodableValue(offset_));
  list.push_back(value_ ? EncodableValue(*value_) : EncodableValue());
  return list;
}

AttRequest AttRequest::FromEncodableList(const EncodableList& list) {
  AttRequest decoded(
    std::get<int64_t>(list[0]),
    std::get<std::string>(list[1]),
    std::get<std::string>(list[2]),
    std::get<std::string>(list[3]),
    std::get<int64_t>(list[4]));
  auto& encodable_value = list[5];
  if (!encodable_value.IsNull()) {
    decoded.set_value(std::get<std::vector<uint8_t>>(encodable_value));
  }
  return decoded;
}

bool AttRequest::operator==(const AttRequest& other) const {
  return PigeonInternalDeepEquals(request_id_, other.request_id_) && PigeonInternalDeepEquals(central_identifier_, other.central_identifier_) && PigeonInternalDeepEquals(characteristic_uuid_, other.characteristic_uuid_) && PigeonInternalDeepEquals(service_uuid_, other.service_uuid_) && PigeonInternalDeepEquals(offset_, other.offset_) && PigeonInternalDeepEquals(value_, other.value_);
}

bool AttRequest::operator!=(const AttRequest& other) const {
  return !(*this == other);
}

size_t AttRequest::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(request_id_);
  result = result * 31 + PigeonInternalDeepHash(central_identifier_);
  result = result * 31 + PigeonInternalDeepHash(characteristic_uuid_);
  result = result * 31 + PigeonInternalDeepHash(service_uuid_);
  result = result * 31 + PigeonInternalDeepHash(offset_);
  result = result * 31 + PigeonInternalDeepHash(value_);
  return result;
}

size_t PigeonInternalDeepHash(const AttRequest& v) {
  return v.Hash();
}

// MutableDescriptor

MutableDescriptor::MutableDescriptor(const std::string& uuid)
 : uuid_(uuid) {}

MutableDescriptor::MutableDescriptor(
  const std::string& uuid,
  const std::vector<uint8_t>* value)
 : uuid_(uuid),
    value_(value ? std::optional<std::vector<uint8_t>>(*value) : std::nullopt) {}

const std::string& MutableDescriptor::uuid() const {
  return uuid_;
}

void MutableDescriptor::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


const std::vector<uint8_t>* MutableDescriptor::value() const {
  return value_ ? &(*value_) : nullptr;
}

void MutableDescriptor::set_value(const std::vector<uint8_t>* value_arg) {
  value_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void MutableDescriptor::set_value(const std::vector<uint8_t>& value_arg) {
  value_ = value_arg;
}


EncodableList MutableDescriptor::ToEncodableList() const {
  EncodableList list;
  list.reserve(2);
  list.push_back(EncodableValue(uuid_));
  list.push_back(value_ ? EncodableValue(*value_) : EncodableValue());
  return list;
}

MutableDescriptor MutableDescriptor::FromEncodableList(const EncodableList& list) {
  MutableDescriptor decoded(
    std::get<std::string>(list[0]));
  auto& encodable_value = list[1];
  if (!encodable_value.IsNull()) {
    decoded.set_value(std::get<std::vector<uint8_t>>(encodable_value));
  }
  return decoded;
}

bool MutableDescriptor::operator==(const MutableDescriptor& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(value_, other.value_);
}

bool MutableDescriptor::operator!=(const MutableDescriptor& other) const {
  return !(*this == other);
}

size_t MutableDescriptor::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(value_);
  return result;
}

size_t PigeonInternalDeepHash(const MutableDescriptor& v) {
  return v.Hash();
}

// MutableCharacteristic

MutableCharacteristic::MutableCharacteristic(const std::string& uuid)
 : uuid_(uuid) {}

MutableCharacteristic::MutableCharacteristic(
  const std::string& uuid,
  const CharacteristicProperty* properties,
  const CharacteristicPermission* permissions,
  const std::vector<uint8_t>* value,
  const EncodableList* descriptors)
 : uuid_(uuid),
    properties_(properties ? std::make_unique<CharacteristicProperty>(*properties) : nullptr),
    permissions_(permissions ? std::make_unique<CharacteristicPermission>(*permissions) : nullptr),
    value_(value ? std::optional<std::vector<uint8_t>>(*value) : std::nullopt),
    descriptors_(descriptors ? std::optional<EncodableList>(*descriptors) : std::nullopt) {}

MutableCharacteristic::MutableCharacteristic(const MutableCharacteristic& other)
 : uuid_(other.uuid_),
    properties_(other.properties_ ? std::make_unique<CharacteristicProperty>(*other.properties_) : nullptr),
    permissions_(other.permissions_ ? std::make_unique<CharacteristicPermission>(*other.permissions_) : nullptr),
    value_(other.value_ ? std::optional<std::vector<uint8_t>>(*other.value_) : std::nullopt),
    descriptors_(other.descriptors_ ? std::optional<EncodableList>(*other.descriptors_) : std::nullopt) {}

MutableCharacteristic& MutableCharacteristic::operator=(const MutableCharacteristic& other) {
  uuid_ = other.uuid_;
  properties_ = other.properties_ ? std::make_unique<CharacteristicProperty>(*other.properties_) : nullptr;
  permissions_ = other.permissions_ ? std::make_unique<CharacteristicPermission>(*other.permissions_) : nullptr;
  value_ = other.value_;
  descriptors_ = other.descriptors_;
  return *this;
}

const std::string& MutableCharacteristic::uuid() const {
  return uuid_;
}

void MutableCharacteristic::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


const CharacteristicProperty* MutableCharacteristic::properties() const {
  return properties_.get();
}

void MutableCharacteristic::set_properties(const CharacteristicProperty* value_arg) {
  properties_ = value_arg ? std::make_unique<CharacteristicProperty>(*value_arg) : nullptr;
}

void MutableCharacteristic::set_properties(const CharacteristicProperty& value_arg) {
  properties_ = std::make_unique<CharacteristicProperty>(value_arg);
}


const CharacteristicPermission* MutableCharacteristic::permissions() const {
  return permissions_.get();
}

void MutableCharacteristic::set_permissions(const CharacteristicPermission* value_arg) {
  permissions_ = value_arg ? std::make_unique<CharacteristicPermission>(*value_arg) : nullptr;
}

void MutableCharacteristic::set_permissions(const CharacteristicPermission& value_arg) {
  permissions_ = std::make_unique<CharacteristicPermission>(value_arg);
}


const std::vector<uint8_t>* MutableCharacteristic::value() const {
  return value_ ? &(*value_) : nullptr;
}

void MutableCharacteristic::set_value(const std::vector<uint8_t>* value_arg) {
  value_ = value_arg ? std::optional<std::vector<uint8_t>>(*value_arg) : std::nullopt;
}

void MutableCharacteristic::set_value(const std::vector<uint8_t>& value_arg) {
  value_ = value_arg;
}


const EncodableList* MutableCharacteristic::descriptors() const {
  return descriptors_ ? &(*descriptors_) : nullptr;
}

void MutableCharacteristic::set_descriptors(const EncodableList* value_arg) {
  descriptors_ = value_arg ? std::optional<EncodableList>(*value_arg) : std::nullopt;
}

void MutableCharacteristic::set_descriptors(const EncodableList& value_arg) {
  descriptors_ = value_arg;
}


EncodableList MutableCharacteristic::ToEncodableList() const {
  EncodableList list;
  list.reserve(5);
  list.push_back(EncodableValue(uuid_));
  list.push_back(properties_ ? CustomEncodableValue(*properties_) : EncodableValue());
  list.push_back(permissions_ ? CustomEncodableValue(*permissions_) : EncodableValue());
  list.push_back(value_ ? EncodableValue(*value_) : EncodableValue());
  list.push_back(descriptors_ ? EncodableValue(*descriptors_) : EncodableValue());
  return list;
}

MutableCharacteristic MutableCharacteristic::FromEncodableList(const EncodableList& list) {
  MutableCharacteristic decoded(
    std::get<std::string>(list[0]));
  auto& encodable_properties = list[1];
  if (!encodable_properties.IsNull()) {
    decoded.set_properties(std::any_cast<const CharacteristicProperty&>(std::get<CustomEncodableValue>(encodable_properties)));
  }
  auto& encodable_permissions = list[2];
  if (!encodable_permissions.IsNull()) {
    decoded.set_permissions(std::any_cast<const CharacteristicPermission&>(std::get<CustomEncodableValue>(encodable_permissions)));
  }
  auto& encodable_value = list[3];
  if (!encodable_value.IsNull()) {
    decoded.set_value(std::get<std::vector<uint8_t>>(encodable_value));
  }
  auto& encodable_descriptors = list[4];
  if (!encodable_descriptors.IsNull()) {
    decoded.set_descriptors(std::get<EncodableList>(encodable_descriptors));
  }
  return decoded;
}

bool MutableCharacteristic::operator==(const MutableCharacteristic& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(properties_, other.properties_) && PigeonInternalDeepEquals(permissions_, other.permissions_) && PigeonInternalDeepEquals(value_, other.value_) && PigeonInternalDeepEquals(descriptors_, other.descriptors_);
}

bool MutableCharacteristic::operator!=(const MutableCharacteristic& other) const {
  return !(*this == other);
}

size_t MutableCharacteristic::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(properties_);
  result = result * 31 + PigeonInternalDeepHash(permissions_);
  result = result * 31 + PigeonInternalDeepHash(value_);
  result = result * 31 + PigeonInternalDeepHash(descriptors_);
  return result;
}

size_t PigeonInternalDeepHash(const MutableCharacteristic& v) {
  return v.Hash();
}

// MutableService

MutableService::MutableService(
  const std::string& uuid,
  bool is_primary,
  const EncodableList& characteristics)
 : uuid_(uuid),
    is_primary_(is_primary),
    characteristics_(characteristics) {}

const std::string& MutableService::uuid() const {
  return uuid_;
}

void MutableService::set_uuid(std::string_view value_arg) {
  uuid_ = value_arg;
}


bool MutableService::is_primary() const {
  return is_primary_;
}

void MutableService::set_is_primary(bool value_arg) {
  is_primary_ = value_arg;
}


const EncodableList& MutableService::characteristics() const {
  return characteristics_;
}

void MutableService::set_characteristics(const EncodableList& value_arg) {
  characteristics_ = value_arg;
}


EncodableList MutableService::ToEncodableList() const {
  EncodableList list;
  list.reserve(3);
  list.push_back(EncodableValue(uuid_));
  list.push_back(EncodableValue(is_primary_));
  list.push_back(EncodableValue(characteristics_));
  return list;
}

MutableService MutableService::FromEncodableList(const EncodableList& list) {
  MutableService decoded(
    std::get<std::string>(list[0]),
    std::get<bool>(list[1]),
    std::get<EncodableList>(list[2]));
  return decoded;
}

bool MutableService::operator==(const MutableService& other) const {
  return PigeonInternalDeepEquals(uuid_, other.uuid_) && PigeonInternalDeepEquals(is_primary_, other.is_primary_) && PigeonInternalDeepEquals(characteristics_, other.characteristics_);
}

bool MutableService::operator!=(const MutableService& other) const {
  return !(*this == other);
}

size_t MutableService::Hash() const {
  size_t result = 1;
  result = result * 31 + PigeonInternalDeepHash(uuid_);
  result = result * 31 + PigeonInternalDeepHash(is_primary_);
  result = result * 31 + PigeonInternalDeepHash(characteristics_);
  return result;
}

size_t PigeonInternalDeepHash(const MutableService& v) {
  return v.Hash();
}


PigeonInternalCodecSerializer::PigeonInternalCodecSerializer() {}

EncodableValue PigeonInternalCodecSerializer::ReadValueOfType(
  uint8_t type,
  ::flutter::ByteStreamReader* stream) const {
  switch (type) {
    case 129: {
        const auto& encodable_enum_arg = ReadValue(stream);
        const int64_t enum_arg_value = encodable_enum_arg.IsNull() ? 0 : encodable_enum_arg.LongValue();
        return encodable_enum_arg.IsNull() ? EncodableValue() : CustomEncodableValue(static_cast<ClientState>(enum_arg_value));
      }
    case 130: {
        const auto& encodable_enum_arg = ReadValue(stream);
        const int64_t enum_arg_value = encodable_enum_arg.IsNull() ? 0 : encodable_enum_arg.LongValue();
        return encodable_enum_arg.IsNull() ? EncodableValue() : CustomEncodableValue(static_cast<ConnectionState>(enum_arg_value));
      }
    case 131: {
        const auto& encodable_enum_arg = ReadValue(stream);
        const int64_t enum_arg_value = encodable_enum_arg.IsNull() ? 0 : encodable_enum_arg.LongValue();
        return encodable_enum_arg.IsNull() ? EncodableValue() : CustomEncodableValue(static_cast<AttResult>(enum_arg_value));
      }
    case 132: {
        return CustomEncodableValue(ClientSession::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 133: {
        return CustomEncodableValue(PeripheralSession::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 134: {
        return CustomEncodableValue(PeripheralManagerSession::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 135: {
        return CustomEncodableValue(Peripheral::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 136: {
        return CustomEncodableValue(AdvertisementData::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 137: {
        return CustomEncodableValue(ScanResult::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 138: {
        return CustomEncodableValue(Service::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 139: {
        return CustomEncodableValue(Characteristic::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 140: {
        return CustomEncodableValue(Descriptor::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 141: {
        return CustomEncodableValue(CharacteristicProperty::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 142: {
        return CustomEncodableValue(CharacteristicPermission::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 143: {
        return CustomEncodableValue(AttRequest::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 144: {
        return CustomEncodableValue(MutableDescriptor::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 145: {
        return CustomEncodableValue(MutableCharacteristic::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 146: {
        return CustomEncodableValue(MutableService::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    default:
      return ::flutter::StandardCodecSerializer::ReadValueOfType(type, stream);
    }
}

void PigeonInternalCodecSerializer::WriteValue(
  const EncodableValue& value,
  ::flutter::ByteStreamWriter* stream) const {
  if (const CustomEncodableValue* custom_value = std::get_if<CustomEncodableValue>(&value)) {
    if (custom_value->type() == typeid(ClientState)) {
      stream->WriteByte(129);
      WriteValue(EncodableValue(static_cast<int>(std::any_cast<ClientState>(*custom_value))), stream);
      return;
    }
    if (custom_value->type() == typeid(ConnectionState)) {
      stream->WriteByte(130);
      WriteValue(EncodableValue(static_cast<int>(std::any_cast<ConnectionState>(*custom_value))), stream);
      return;
    }
    if (custom_value->type() == typeid(AttResult)) {
      stream->WriteByte(131);
      WriteValue(EncodableValue(static_cast<int>(std::any_cast<AttResult>(*custom_value))), stream);
      return;
    }
    if (custom_value->type() == typeid(ClientSession)) {
      stream->WriteByte(132);
      WriteValue(EncodableValue(std::any_cast<ClientSession>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(PeripheralSession)) {
      stream->WriteByte(133);
      WriteValue(EncodableValue(std::any_cast<PeripheralSession>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(PeripheralManagerSession)) {
      stream->WriteByte(134);
      WriteValue(EncodableValue(std::any_cast<PeripheralManagerSession>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(Peripheral)) {
      stream->WriteByte(135);
      WriteValue(EncodableValue(std::any_cast<Peripheral>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(AdvertisementData)) {
      stream->WriteByte(136);
      WriteValue(EncodableValue(std::any_cast<AdvertisementData>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(ScanResult)) {
      stream->WriteByte(137);
      WriteValue(EncodableValue(std::any_cast<ScanResult>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(Service)) {
      stream->WriteByte(138);
      WriteValue(EncodableValue(std::any_cast<Service>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(Characteristic)) {
      stream->WriteByte(139);
      WriteValue(EncodableValue(std::any_cast<Characteristic>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(Descriptor)) {
      stream->WriteByte(140);
      WriteValue(EncodableValue(std::any_cast<Descriptor>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(CharacteristicProperty)) {
      stream->WriteByte(141);
      WriteValue(EncodableValue(std::any_cast<CharacteristicProperty>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(CharacteristicPermission)) {
      stream->WriteByte(142);
      WriteValue(EncodableValue(std::any_cast<CharacteristicPermission>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(AttRequest)) {
      stream->WriteByte(143);
      WriteValue(EncodableValue(std::any_cast<AttRequest>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(MutableDescriptor)) {
      stream->WriteByte(144);
      WriteValue(EncodableValue(std::any_cast<MutableDescriptor>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(MutableCharacteristic)) {
      stream->WriteByte(145);
      WriteValue(EncodableValue(std::any_cast<MutableCharacteristic>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(MutableService)) {
      stream->WriteByte(146);
      WriteValue(EncodableValue(std::any_cast<MutableService>(*custom_value).ToEncodableList()), stream);
      return;
    }
  }
  ::flutter::StandardCodecSerializer::WriteValue(value, stream);
}

/// The codec used by ButaneHostApi.
const ::flutter::StandardMessageCodec& ButaneHostApi::GetCodec() {
  return ::flutter::StandardMessageCodec::GetInstance(&PigeonInternalCodecSerializer::GetInstance());
}

// Sets up an instance of `ButaneHostApi` to handle messages through the `binary_messenger`.
void ButaneHostApi::SetUp(
  ::flutter::BinaryMessenger* binary_messenger,
  ButaneHostApi* api) {
  ButaneHostApi::SetUp(binary_messenger, api, "");
}

void ButaneHostApi::SetUp(
  ::flutter::BinaryMessenger* binary_messenger,
  ButaneHostApi* api,
  const std::string& message_channel_suffix) {
  const std::string prepended_suffix = message_channel_suffix.length() > 0 ? std::string(".") + message_channel_suffix : "";
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.state" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          const auto* session_arg = encodable_session_arg.IsNull() ? nullptr : &(std::any_cast<const ClientSession&>(std::get<CustomEncodableValue>(encodable_session_arg)));
          api->State(session_arg, [reply](ErrorOr<ClientState>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(CustomEncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.scan" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          const auto* session_arg = encodable_session_arg.IsNull() ? nullptr : &(std::any_cast<const ClientSession&>(std::get<CustomEncodableValue>(encodable_session_arg)));
          const auto& encodable_for_services_arg = args.at(1);
          const auto* for_services_arg = std::get_if<EncodableList>(&encodable_for_services_arg);
          api->Scan(session_arg, for_services_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.cancelScan" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          const auto* session_arg = encodable_session_arg.IsNull() ? nullptr : &(std::any_cast<const ClientSession&>(std::get<CustomEncodableValue>(encodable_session_arg)));
          api->CancelScan(session_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.peripherals" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          const auto* session_arg = encodable_session_arg.IsNull() ? nullptr : &(std::any_cast<const ClientSession&>(std::get<CustomEncodableValue>(encodable_session_arg)));
          const auto& encodable_peripheral_identifiers_arg = args.at(1);
          if (encodable_peripheral_identifiers_arg.IsNull()) {
            reply(WrapError("peripheral_identifiers_arg unexpectedly null."));
            return;
          }
          const auto& peripheral_identifiers_arg = std::get<EncodableList>(encodable_peripheral_identifiers_arg);
          api->Peripherals(session_arg, peripheral_identifiers_arg, [reply](ErrorOr<EncodableList>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.connectedPeripherals" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          const auto* session_arg = encodable_session_arg.IsNull() ? nullptr : &(std::any_cast<const ClientSession&>(std::get<CustomEncodableValue>(encodable_session_arg)));
          const auto& encodable_service_uuids_arg = args.at(1);
          if (encodable_service_uuids_arg.IsNull()) {
            reply(WrapError("service_uuids_arg unexpectedly null."));
            return;
          }
          const auto& service_uuids_arg = std::get<EncodableList>(encodable_service_uuids_arg);
          api->ConnectedPeripherals(session_arg, service_uuids_arg, [reply](ErrorOr<EncodableList>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.connect" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->Connect(session_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.cancelConnection" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->CancelConnection(session_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.connectionState" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->ConnectionState(session_arg, [reply](ErrorOr<::butane_windows::ConnectionState>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(CustomEncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.discoverServices" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuids_arg = args.at(1);
          const auto* service_uuids_arg = std::get_if<EncodableList>(&encodable_service_uuids_arg);
          api->DiscoverServices(session_arg, service_uuids_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.services" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->Services(session_arg, [reply](ErrorOr<EncodableList>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.discoverCharacteristics" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuids_arg = args.at(2);
          const auto* characteristic_uuids_arg = std::get_if<EncodableList>(&encodable_characteristic_uuids_arg);
          api->DiscoverCharacteristics(session_arg, service_uuid_arg, characteristic_uuids_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.characteristics" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          api->Characteristics(session_arg, service_uuid_arg, [reply](ErrorOr<EncodableList>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.readCharacteristic" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(2);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          api->ReadCharacteristic(session_arg, service_uuid_arg, characteristic_uuid_arg, [reply](ErrorOr<std::vector<uint8_t>>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.writeCharacteristic" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(2);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          const auto& encodable_value_arg = args.at(3);
          if (encodable_value_arg.IsNull()) {
            reply(WrapError("value_arg unexpectedly null."));
            return;
          }
          const auto& value_arg = std::get<std::vector<uint8_t>>(encodable_value_arg);
          const auto& encodable_without_response_arg = args.at(4);
          if (encodable_without_response_arg.IsNull()) {
            reply(WrapError("without_response_arg unexpectedly null."));
            return;
          }
          const auto& without_response_arg = std::get<bool>(encodable_without_response_arg);
          api->WriteCharacteristic(session_arg, service_uuid_arg, characteristic_uuid_arg, value_arg, without_response_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.observeCharacteristic" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_observe_arg = args.at(0);
          if (encodable_observe_arg.IsNull()) {
            reply(WrapError("observe_arg unexpectedly null."));
            return;
          }
          const auto& observe_arg = std::get<bool>(encodable_observe_arg);
          const auto& encodable_session_arg = args.at(1);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(2);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(3);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          api->ObserveCharacteristic(observe_arg, session_arg, service_uuid_arg, characteristic_uuid_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.readDescriptor" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(2);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          const auto& encodable_descriptor_uuid_arg = args.at(3);
          if (encodable_descriptor_uuid_arg.IsNull()) {
            reply(WrapError("descriptor_uuid_arg unexpectedly null."));
            return;
          }
          const auto& descriptor_uuid_arg = std::get<std::string>(encodable_descriptor_uuid_arg);
          api->ReadDescriptor(session_arg, service_uuid_arg, characteristic_uuid_arg, descriptor_uuid_arg, [reply](ErrorOr<std::vector<uint8_t>>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.writeDescriptor" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(2);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          const auto& encodable_descriptor_uuid_arg = args.at(3);
          if (encodable_descriptor_uuid_arg.IsNull()) {
            reply(WrapError("descriptor_uuid_arg unexpectedly null."));
            return;
          }
          const auto& descriptor_uuid_arg = std::get<std::string>(encodable_descriptor_uuid_arg);
          const auto& encodable_value_arg = args.at(4);
          if (encodable_value_arg.IsNull()) {
            reply(WrapError("value_arg unexpectedly null."));
            return;
          }
          const auto& value_arg = std::get<std::vector<uint8_t>>(encodable_value_arg);
          api->WriteDescriptor(session_arg, service_uuid_arg, characteristic_uuid_arg, descriptor_uuid_arg, value_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.readRssi" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->ReadRssi(session_arg, [reply](ErrorOr<int64_t>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.requestMtu" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_mtu_arg = args.at(1);
          if (encodable_mtu_arg.IsNull()) {
            reply(WrapError("mtu_arg unexpectedly null."));
            return;
          }
          const int64_t mtu_arg = encodable_mtu_arg.LongValue();
          api->RequestMtu(session_arg, mtu_arg, [reply](ErrorOr<int64_t>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.peripheralManagerState" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->PeripheralManagerState(session_arg, [reply](ErrorOr<ClientState>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(CustomEncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.startAdvertising" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_local_name_arg = args.at(1);
          const auto* local_name_arg = std::get_if<std::string>(&encodable_local_name_arg);
          const auto& encodable_service_uuids_arg = args.at(2);
          const auto* service_uuids_arg = std::get_if<EncodableList>(&encodable_service_uuids_arg);
          api->StartAdvertising(session_arg, local_name_arg, service_uuids_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.stopAdvertising" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->StopAdvertising(session_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.addService" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_arg = args.at(1);
          if (encodable_service_arg.IsNull()) {
            reply(WrapError("service_arg unexpectedly null."));
            return;
          }
          const auto& service_arg = std::any_cast<const MutableService&>(std::get<CustomEncodableValue>(encodable_service_arg));
          api->AddService(session_arg, service_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.removeService" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          api->RemoveService(session_arg, service_uuid_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.removeAllServices" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          api->RemoveAllServices(session_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.respondToRequest" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_request_id_arg = args.at(1);
          if (encodable_request_id_arg.IsNull()) {
            reply(WrapError("request_id_arg unexpectedly null."));
            return;
          }
          const int64_t request_id_arg = encodable_request_id_arg.LongValue();
          const auto& encodable_request_result_arg = args.at(2);
          if (encodable_request_result_arg.IsNull()) {
            reply(WrapError("request_result_arg unexpectedly null."));
            return;
          }
          const auto& request_result_arg = std::any_cast<const AttResult&>(std::get<CustomEncodableValue>(encodable_request_result_arg));
          const auto& encodable_value_arg = args.at(3);
          const auto* value_arg = std::get_if<std::vector<uint8_t>>(&encodable_value_arg);
          api->RespondToRequest(session_arg, request_id_arg, request_result_arg, value_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.butane_platform_interface.ButaneHostApi.updateValue" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const ::flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_session_arg = args.at(0);
          if (encodable_session_arg.IsNull()) {
            reply(WrapError("session_arg unexpectedly null."));
            return;
          }
          const auto& session_arg = std::any_cast<const PeripheralManagerSession&>(std::get<CustomEncodableValue>(encodable_session_arg));
          const auto& encodable_service_uuid_arg = args.at(1);
          if (encodable_service_uuid_arg.IsNull()) {
            reply(WrapError("service_uuid_arg unexpectedly null."));
            return;
          }
          const auto& service_uuid_arg = std::get<std::string>(encodable_service_uuid_arg);
          const auto& encodable_characteristic_uuid_arg = args.at(2);
          if (encodable_characteristic_uuid_arg.IsNull()) {
            reply(WrapError("characteristic_uuid_arg unexpectedly null."));
            return;
          }
          const auto& characteristic_uuid_arg = std::get<std::string>(encodable_characteristic_uuid_arg);
          const auto& encodable_value_arg = args.at(3);
          if (encodable_value_arg.IsNull()) {
            reply(WrapError("value_arg unexpectedly null."));
            return;
          }
          const auto& value_arg = std::get<std::vector<uint8_t>>(encodable_value_arg);
          api->UpdateValue(session_arg, service_uuid_arg, characteristic_uuid_arg, value_arg, [reply](ErrorOr<bool>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
}

EncodableValue ButaneHostApi::WrapError(std::string_view error_message) {
  return EncodableValue(EncodableList{
    EncodableValue(std::string(error_message)),
    EncodableValue("Error"),
    EncodableValue()
  });
}

EncodableValue ButaneHostApi::WrapError(const FlutterError& error) {
  return EncodableValue(EncodableList{
    EncodableValue(error.code()),
    EncodableValue(error.message()),
    error.details()
  });
}

// Generated class from Pigeon that represents Flutter messages that can be called from C++.
ButaneFlutterApi::ButaneFlutterApi(::flutter::BinaryMessenger* binary_messenger)
 : binary_messenger_(binary_messenger),
    message_channel_suffix_("") {}

ButaneFlutterApi::ButaneFlutterApi(
  ::flutter::BinaryMessenger* binary_messenger,
  const std::string& message_channel_suffix)
 : binary_messenger_(binary_messenger),
    message_channel_suffix_(message_channel_suffix.length() > 0 ? std::string(".") + message_channel_suffix : "") {}

const ::flutter::StandardMessageCodec& ButaneFlutterApi::GetCodec() {
  return ::flutter::StandardMessageCodec::GetInstance(&PigeonInternalCodecSerializer::GetInstance());
}

void ButaneFlutterApi::OnClientState(
  const std::string* client_identifier_arg,
  const ClientState& state_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onClientState" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    client_identifier_arg ? EncodableValue(*client_identifier_arg) : EncodableValue(),
    CustomEncodableValue(state_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnScanResult(
  const ScanResult& scan_result_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onScanResult" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(scan_result_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnConnectionState(
  const Peripheral& peripheral_arg,
  const ConnectionState& state_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onConnectionState" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(peripheral_arg),
    CustomEncodableValue(state_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnCharacteristicValue(
  const Peripheral& peripheral_arg,
  const Characteristic& characteristic_arg,
  const std::vector<uint8_t>& value_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onCharacteristicValue" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(peripheral_arg),
    CustomEncodableValue(characteristic_arg),
    EncodableValue(value_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnDescriptorValue(
  const Peripheral& peripheral_arg,
  const Descriptor& descriptor_arg,
  const std::vector<uint8_t>& value_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onDescriptorValue" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(peripheral_arg),
    CustomEncodableValue(descriptor_arg),
    EncodableValue(value_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnPeripheralManagerState(
  const std::string* client_identifier_arg,
  const ClientState& state_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onPeripheralManagerState" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    client_identifier_arg ? EncodableValue(*client_identifier_arg) : EncodableValue(),
    CustomEncodableValue(state_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnServiceAdded(
  const std::string& service_uuid_arg,
  const std::string* error_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onServiceAdded" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    EncodableValue(service_uuid_arg),
    error_arg ? EncodableValue(*error_arg) : EncodableValue(),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnReadRequest(
  const AttRequest& request_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onReadRequest" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(request_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnWriteRequests(
  const EncodableList& requests_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onWriteRequests" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    EncodableValue(requests_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnCentralSubscribed(
  const std::string* client_identifier_arg,
  const std::string& central_identifier_arg,
  const std::string& service_uuid_arg,
  const std::string& characteristic_uuid_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onCentralSubscribed" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    client_identifier_arg ? EncodableValue(*client_identifier_arg) : EncodableValue(),
    EncodableValue(central_identifier_arg),
    EncodableValue(service_uuid_arg),
    EncodableValue(characteristic_uuid_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnCentralUnsubscribed(
  const std::string* client_identifier_arg,
  const std::string& central_identifier_arg,
  const std::string& service_uuid_arg,
  const std::string& characteristic_uuid_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onCentralUnsubscribed" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    client_identifier_arg ? EncodableValue(*client_identifier_arg) : EncodableValue(),
    EncodableValue(central_identifier_arg),
    EncodableValue(service_uuid_arg),
    EncodableValue(characteristic_uuid_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void ButaneFlutterApi::OnReadyToUpdateSubscribers(
  const std::string* client_identifier_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.butane_platform_interface.ButaneFlutterApi.onReadyToUpdateSubscribers" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    client_identifier_arg ? EncodableValue(*client_identifier_arg) : EncodableValue(),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

}  // namespace butane_windows
