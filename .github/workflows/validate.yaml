name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  dependency-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate tracked dependency state
        run: bash tool/validate.sh --repo
      - name: Test dependency validator
        run: bash tool/validate_test.sh
